{
  "name": "@juice-editor/draw",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "copyfiles -u 2 --all 'drawio/webapp/**/*' dist/drawio"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
